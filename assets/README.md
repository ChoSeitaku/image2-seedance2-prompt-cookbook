# assets

This directory stores shared assets such as logos, banners, and other media files used across the project.

Do not place prompt preview images here. Preview images belong alongside their corresponding `prompt.json` files.
